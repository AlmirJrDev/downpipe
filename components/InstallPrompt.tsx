// Instalar na tela inicial é assunto de navegador. No app nativo a pessoa
// já baixou — só a versão web (InstallPrompt.web.tsx) desenha alguma coisa.
export function InstallPrompt() {
  return null;
}
