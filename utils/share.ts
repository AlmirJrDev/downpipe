// No celular é o compartilhamento do sistema. O par (share.web.ts) usa a
// Web Share API, com cópia pro clipboard onde ela não existe — desktop, na
// maioria dos casos.
export { Share } from "react-native";
